# Base image for all services
FROM node:20-alpine

WORKDIR /app

# Install production dependencies
RUN apk add --no-cache \
    python3 \
    make \
    g++

COPY package*.json ./
COPY tsconfig.json ./

RUN npm ci --omit=dev

COPY . .

RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
